/* Port the server will be listening on */
const port = 25000

/* Dir for frontend views */
const rootviews = __dirname + "/views"

module.exports = {
    PORT       : port,
    VIEWS      : rootviews
}